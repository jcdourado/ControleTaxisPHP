CREATE TABLE USERS(
	ID INT AUTO_INCREMENT PRIMARY KEY,
	NOME VARCHAR(20) UNIQUE,
	SENHA VARCHAR(30)
);

CREATE TABLE TAXIS(
	ID INT AUTO_INCREMENT PRIMARY KEY,
	NOME VARCHAR(30),
	PLACA VARCHAR(8),
	BAIRRO VARCHAR(30),
	CIDADE VARCHAR(20),
	ESTADO VARCHAR(3)
)